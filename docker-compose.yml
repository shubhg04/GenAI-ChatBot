services:
  backend:
    build: .
    container_name: aichatbot-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/datas

    restart: unless-stopped